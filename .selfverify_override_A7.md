# Override manual — f3_selfverify.sh check(d), sub-item A7 (SD#58)

**Fecha:** 2026-07-06
**Autorizado por:** Miguel Rodríguez ("Confirmo", en respuesta a la presentación explícita de este override para A7 específicamente).

## Por qué se fuerza el marker

Mismo patrón que A4/A5: el diff de A7 parte de la branch de A4 (que ya toca 8
app-dirs), así que `f3_selfverify.sh` cae a pytest full-repo por la heurística #163
(>3 apps tocadas acumuladas).

El full-repo da **220 failed / 123 errors**, 100% preexistentes y ajenos a este
sub-item. Verificado independientemente por el orquestador con git stash A/B,
aislando SOLO el diff de A7 (`apps/certifications/views.py`,
`templates/certifications/my_certificates.html`,
`apps/certifications/tests/test_issue_58_a7.py`) contra el commit base `2bd6055` (A4):

- **Baseline** (diff de A7 stasheado): `220 failed, 789 passed, 1 skipped, 123 errors`
- **Con A7** (diff restaurado): `220 failed, 802 passed, 1 skipped, 123 errors`
- **Diff exacto del set de 220 FAILED (`grep ^FAILED | sort | diff`): 0 diferencias.**
  Mismos tests, mismo orden, en ambos lados. La única diferencia son +13 tests nuevos
  de A7, todos pasando (789+13=802 ✓).

## Verificación propia (scoped)

```
manage.py test apps.accounts apps.certifications
# -> 190/190 verde
```

`manage.py check`: limpio. `makemigrations --check --dry-run`: sin cambios pendientes.

## Qué NO cubre este override

Específico al check(d) de A7 en este worktree. No exime a A6/A8/A9/A10 de su propia
verificación y confirmación explícita de Miguel antes de forzar sus respectivos markers.
