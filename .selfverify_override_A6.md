# Override manual — f3_selfverify.sh check(d), sub-item A6 (SD#58)

**Fecha:** 2026-07-06
**Autorizado por:** Miguel Rodríguez ("Si", en respuesta a la presentación explícita de este override para A6 específicamente).

## Por qué se fuerza el marker

Mismo patrón que A4/A5/A7: el diff de A6 parte de la branch de A4 (que ya toca 8
app-dirs), así que `f3_selfverify.sh` cae a pytest full-repo por la heurística #163
(>3 apps tocadas acumuladas).

El full-repo da **220 failed / 123 errors**, 100% preexistentes y ajenos a este
sub-item. Verificado independientemente por el orquestador con git stash A/B,
aislando SOLO el diff de A6 (`apps/courses/views.py`, `apps/learning_paths/views.py`,
`apps/assessments/views.py` + 3 archivos de tests nuevos) contra el commit base
`2bd6055` (A4):

- **Baseline** (diff de A6 stasheado): `220 failed, 789 passed, 1 skipped, 123 errors`
- **Con A6** (diff restaurado): `220 failed, 808 passed, 1 skipped, 123 errors`
- **Diff exacto del set de 220 FAILED (`grep ^FAILED | sort | diff`): 0 diferencias.**
  Mismos tests, mismo orden. La única diferencia son +19 tests nuevos de A6, todos
  pasando (789+19=808 ✓).

También se confirmó, aislando el diff, que los 7 `ERROR` scoped en
`apps.learning_paths.tests.test_api` (bug preexistente de `LearningPath.total_duration`
referenciando un campo de `Course` inexistente) son idénticos con y sin el diff de A6
(290→309 tests, mismos 7 errores en ambos lados).

## Verificación propia (scoped)

```
manage.py test apps.accounts apps.courses apps.learning_paths apps.assessments
# -> 309/309 OK aparte de los 7 ERROR preexistentes confirmados idénticos
```

`manage.py check`: limpio. `makemigrations --check --dry-run`: sin cambios pendientes.

## Qué NO cubre este override

Específico al check(d) de A6 en este worktree. No exime a A8/A9/A10 de su propia
verificación y confirmación explícita de Miguel antes de forzar sus respectivos markers.
