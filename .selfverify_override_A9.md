# Override manual — f3_selfverify.sh check(d), sub-item A9 (SD#58)

**Fecha:** 2026-07-06
**Autorizado por:** Miguel Rodríguez ("Confirmo ambas", en respuesta a la presentación explícita de este override para A8 y A9).

## Por qué se fuerza el marker

Mismo patrón que A4/A5/A6/A7/A8: el diff de A9 parte de la branch de A4 (que ya toca
8 app-dirs), así que `f3_selfverify.sh` cae a pytest full-repo por la heurística #163
(>3 apps tocadas acumuladas).

El full-repo da **220 failed / 123 errors**, 100% preexistentes y ajenos a este
sub-item. Verificado independientemente por el orquestador con git stash A/B,
aislando SOLO el diff de A9 (`apps/gamification/services/dashboard.py`,
`apps/gamification/views.py`, `apps/gamification/urls.py`,
`apps/gamification/tests/test_issue_58_a9.py`, `templates/gamification/team/`)
contra el commit base `2bd6055` (A4):

- **Baseline** (diff de A9 stasheado): `220 failed, 789 passed, 1 skipped, 123 errors`
- **Con A9** (diff restaurado): `220 failed, 806 passed, 1 skipped, 123 errors`
- **Diff exacto del set de 220 FAILED (`grep ^FAILED | sort | diff`): 0 diferencias.**
  Mismos tests, mismo orden. La única diferencia son +17 tests nuevos de A9, todos
  pasando (789+17=806 ✓).

## Verificación propia (scoped)

```
manage.py test apps.accounts apps.gamification
# -> 295/295 verde
```

`manage.py check`: limpio. `makemigrations --check --dry-run`: sin cambios pendientes.

## Qué NO cubre este override

Específico al check(d) de A9 en este worktree. No exime a A10 de su propia
verificación y confirmación explícita de Miguel.
