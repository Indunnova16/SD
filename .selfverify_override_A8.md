# Override manual — f3_selfverify.sh check(d), sub-item A8 (SD#58)

**Fecha:** 2026-07-06
**Autorizado por:** Miguel Rodríguez ("Confirmo ambas", en respuesta a la presentación explícita de este override para A8 y A9).

## Por qué se fuerza el marker

Mismo patrón que A4/A5/A6/A7: el diff de A8 parte de la branch de A4 (que ya toca 8
app-dirs), así que `f3_selfverify.sh` cae a pytest full-repo por la heurística #163
(>3 apps tocadas acumuladas).

El full-repo da **220 failed / 123 errors**, 100% preexistentes y ajenos a este
sub-item. Verificado independientemente por el orquestador con git stash A/B,
aislando SOLO el diff de A8 (`apps/preop_talks/views.py`,
`apps/lessons_learned/views.py`, `apps/attendance/views.py`,
`apps/accounts/tests/test_issue_58_a4.py`, `apps/accounts/tests/test_issue_58_a8.py`)
contra el commit base `2bd6055` (A4):

- **Baseline** (diff de A8 stasheado): `220 failed, 789 passed, 1 skipped, 123 errors`
- **Con A8** (diff restaurado): `220 failed, 812 passed, 1 skipped, 123 errors`
- **Diff exacto del set de 220 FAILED (`grep ^FAILED | sort | diff`): 0 diferencias.**
  Mismos tests, mismo orden. La única diferencia son +23 tests nuevos de A8, todos
  pasando (789+23=812 ✓).

## Verificación propia (scoped)

```
manage.py test apps.accounts apps.preop_talks apps.lessons_learned apps.attendance
# -> 149/149 verde
```

`manage.py check`: limpio. `makemigrations --check --dry-run`: sin cambios pendientes.

## Hallazgo colateral (documentado, no arreglado — fuera de scope de A8)

`TalkAttendeeService` no tiene los métodos `sign_attendance`/`add_attendee`/
`remove_attendee` que invoca `apps/preop_talks/views.py` — bug preexistente ajeno a
RBAC que rompe la firma de asistencia a charlas para cualquier rol. Queda para el
comentario de cierre final de #58 (radar de hallazgos colaterales, junto con el
nonce de CSP de A2 y la race condition de Alpine de #57).

## Qué NO cubre este override

Específico al check(d) de A8 en este worktree. No exime a A9/A10 de su propia
verificación (A9 ya verificada por separado) y confirmación explícita de Miguel.
