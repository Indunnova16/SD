# Override manual — f3_selfverify.sh check(d), sub-item A4 (SD#58)

**Fecha:** 2026-07-06
**Autorizado por:** Miguel Rodríguez ("Confirmo ambas", en respuesta a la presentación explícita de este override).

## Por qué se fuerza el marker

`f3_selfverify.sh` cae a pytest full-repo (no scoped) porque el diff de A4 toca 8
app-dirs (>3, umbral de la heurística #163 del propio script — comportamiento
correcto y esperado para un cambio de este alcance, autorizado en F2/PLAN.md).

El full-repo da **220 failed / 123 errors**, pero son **100% preexistentes y ajenos**
a este sub-item. Verificado independientemente por el orquestador (no solo por el
subagente) con git stash A/B contra el commit base `f037bcf`:

- **Baseline** (diff de A4 stasheado): `220 failed, 755 passed, 1 skipped, 123 errors`
- **Con A4** (diff restaurado): `220 failed, 789 passed, 1 skipped, 123 errors`
- **Diff exacto del set de 220 FAILED (`grep ^FAILED | sort | diff`): 0 diferencias.**
  Mismos tests, mismo orden, en ambos lados. La única diferencia es +34 tests
  nuevos de A4, todos pasando (755+34=789 ✓).

El rojo preexistente vive sobre todo en `apps/sync/` (fixtures rotas asignando un
string donde se espera una FK `JobProfileType`, `ValueError: Cannot assign` — bug
de fixture completamente ajeno a RBAC/rol) y otras apps no tocadas por A4.

El script tiene carveouts para otro red ajeno conocido (#56/#187: GDAL, Playwright,
ImportError/"no such column") pero ninguno cubre esta firma de rojo — gap de
cobertura del gate, no un defecto del código de A4. Precedente análogo:
`sdchecklist-hallazgo-tiempo-estimado-tz-bug`, `arcopack-nomina-decimal-unboundlocal-283`.

## Verificación propia (scoped a los 8 apps tocados)

```
manage.py test apps.accounts apps.courses apps.certifications apps.preop_talks \
  apps.lessons_learned apps.attendance apps.reports apps.gamification
# -> 475/475 verde (441 preexistentes + 34 nuevos)
```

`manage.py check`: limpio. `makemigrations --check --dry-run`: sin cambios pendientes
(A4 es code-only, no toca modelos).

## Qué NO cubre este override

Este override es específico al check(d) (tests) de A4 en este worktree. No exime a
sub-items futuros (A5-A10) de correr su propio self-verify normal.
