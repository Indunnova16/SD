# Override manual — f3_selfverify.sh check(d), sub-item A5 (SD#58)

**Fecha:** 2026-07-06
**Autorizado por:** Miguel Rodríguez ("Confirmo ambas", en respuesta a la presentación explícita de este override para A5 específicamente).

## Por qué se fuerza el marker

Mismo patrón que A4 (`.selfverify_override_A4.md` en el worktree de A4): el diff de A5
parte de la branch de A4 (que ya toca 8 app-dirs), así que `f3_selfverify.sh` cae a
pytest full-repo por la misma heurística #163 (>3 apps tocadas acumuladas).

El full-repo da **220 failed / 123 errors**, 100% preexistentes y ajenos a este
sub-item (bug de fixture en `apps/sync`, ajeno a RBAC/navbar). Verificado
independientemente por el orquestador con git stash A/B, aislando SOLO el diff de A5
(`templates/partials/navbar.html` + `apps/accounts/tests/test_issue_58_a5.py`) contra
el commit base `2bd6055` (A4):

- **Baseline** (diff de A5 stasheado): `220 failed, 789 passed, 1 skipped, 123 errors`
- **Con A5** (diff restaurado): `220 failed, 805 passed, 1 skipped, 123 errors`
- **Diff exacto del set de 220 FAILED (`grep ^FAILED | sort | diff`): 0 diferencias.**
  Mismos tests, mismo orden, en ambos lados. La única diferencia son +16 tests nuevos
  de A5, todos pasando (789+16=805 ✓).

## Verificación propia (scoped)

```
manage.py test apps.accounts
# -> 122/122 verde
```

`manage.py check`: limpio. `makemigrations --check --dry-run`: sin cambios pendientes
(A5 es template + tests, no toca modelos).

## Qué NO cubre este override

Específico al check(d) de A5 en este worktree. No exime a sub-items futuros (A6, A7,
A8, A9, A10) de su propio self-verify — aunque es previsible que topen el mismo
false-negative por partir de A4, cada uno requiere su propia verificación y
confirmación explícita de Miguel antes de forzar su marker.
